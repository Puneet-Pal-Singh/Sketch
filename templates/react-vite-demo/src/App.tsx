export default function App() {
  return (
    <main className="starter-shell">
      <section className="starter-card" aria-labelledby="starter-title">
        <p className="starter-mark">SKETCH / PREVIEW</p>
        <h1 id="starter-title">Ready for your idea.</h1>
        <p>
          This is the minimal React and Vite envelope. Keep the dev command and
          development inspector import while building the requested experience.
        </p>
        <button data-studio-id="starter-action" data-studio-source="src/App.tsx" type="button">
          Start building
        </button>
      </section>
    </main>
  );
}
